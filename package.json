{
  "name": "decentralized-voting-project",
  "version": "1.0.0",
  "description": "Full-stack Web3 Voting System with SIWE and Smart Contracts",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "blockchain": "cd blockchain && npx hardhat node",
    "deploy:local": "cd blockchain && npx hardhat run scripts/deploy.js --network localhost",
    "test": "cd blockchain && npx hardhat test"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
